[tool.ruff]
line-length = 100
target-version = "py311"
src = ["projects"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "N"]

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra"
testpaths = ["projects"]
pythonpath = ["projects"]
